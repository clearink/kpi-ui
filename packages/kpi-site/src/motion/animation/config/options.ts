import { eases } from '../../easing'

const Options = {
  easing: eases.easeInSine,
  times: [],

  delay: 0,
  duration: 300,
  autoplay: true,

  repeat: 0,
  repeatType: 'loop',
  repeatDelay: 0,
}

export default Options
