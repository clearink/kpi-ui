import type { NonUndefined, SizeType } from '@kpi/shared'

// eslint-disable-next-line import/prefer-default-export
export const SPACE_SIZE: Record<NonUndefined<SizeType>, number> = {
  small: 8,
  middle: 16,
  large: 24,
}
