// 校验失败声明
/* eslint-disable no-template-curly-in-string */

export const base = {
  required: '${path} is required',
}
export const string = {
  min: '${path} is required',
}
export const number = {
  min: '${path} is required',
}
