# `kpi-ui`

## 如何运行
```
  pnpm install && yarn build && yarn dev:site
```
[在线 Demo](https://clearink.github.io/kpi-ui/)

## TODO

1. 完成 `kpi-motion` 的 animateElement 功能

2. 舍弃之前的打包，编译方式。全部采用rollup打包